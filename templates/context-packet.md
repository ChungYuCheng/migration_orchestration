# Context 封包

> Optional artifact. 預設不獨立落地到 repo。優先把必要上下文內嵌進 `task-brief`；只有在少數高複雜度情況才另外使用。

## 全域摘要
- We are in migration phase, not redesign phase
- Shared contracts are frozen
- Preserve behavior; improve internal structure only where needed
- Do not broaden scope

## 區域摘要
- Current batch:
- Why this batch exists:
- Dependencies already completed:
- Dependencies not yet available:

## 相關摘錄
- 介面摘錄：

```ts
// Paste only the exact interface needed for this task
```

- 共享型別摘錄：

```ts
// Paste only the exact types needed for this task
```

- 既有 pattern 摘錄：

```ts
// Paste one nearby migrated example if available
```

## 保護區域提醒
- Do not edit:
- Escalate if task appears to require changes there
